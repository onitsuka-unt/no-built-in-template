<div class="drawer-menu" aria-hidden="true" data-role="menu">
  <div class="drawer-menuContents" data-role="contents">
    <button type="button" data-role="close">閉じる</button>
    <nav class="drawer-menuNav">
      <ul class="drawer-menuList">
        <li class="drawer-menuItem">
          <a href="/" class="drawer-menuLink">メニュー1</a>
        </li>
        <li class="drawer-menuItem">
          <a href="/" class="drawer-menuLink" target="_blank">メニュー2</a>
        </li>
        <li class="drawer-menuItem">
          <a href="#anchor" class="drawer-menuLink">メニュー3</a>
        </li>
      </ul>
    </nav>
  </div>
  <div class="drawer-menuOverlay" data-role="close"></div>
</div>

<style lang="scss">
  .drawer-menu {
    position: fixed;
    top: 0;
    left: 0;
    z-index: var(--index-fixed);
    display: grid;
    visibility: hidden;
    place-items: center;
    width: 100%;
    height: 100%;
    opacity: 0;
    transition:
      opacity var(--transition-primary) var(--ease-out-expo),
      visibility var(--transition-primary) var(--ease-out-expo);

    &[data-open='true'] {
      visibility: visible;
      opacity: 1;
    }
  }

  .drawer-menuContents {
    padding-block: 2rem;
    padding-inline: 2rem;
    background-color: #fff;
  }

  .drawer-menuOverlay {
    position: absolute;
    inset: 0;
    z-index: -1;
    width: 100vw;
    background-color: rgb(0 0 0 / 30%);
  }
</style>

<script>
  import initializeDrawerMenu from '@/scripts/common/initializeDrawerMenu';

  document.addEventListener('DOMContentLoaded', () => {
    initializeDrawerMenu().init();
  });
</script>
